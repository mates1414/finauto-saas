# FinAuto SaaS web — example configuration. Copy to `.env` (or `.env.local`) for local dev.
# Base URL of the FastAPI backend. Defaults to http://localhost:8000 if unset.
VITE_API_BASE=http://localhost:8000
